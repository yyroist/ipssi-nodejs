<%- include ('../partials/header') %> 

<div class="uk-container uk-margin-top">
    <h1>Informations de l'utilisateur n° <%= user.id %></h1>

    <table class="uk-table uk-table-small uk-table-striped">
        <tbody>
            <tr>
                <th>Nom</th>
                <th><%= user.lastname %></th>
            </tr>
            <tr>
                <th>Prénom</th>
                <th><%= user.firstname %></th>
            </tr>
            <tr>
                <th>Âge</th>
                <th><%= user.age %></th>
            </tr>
        </tbody>
    </table>

    <a href="/users" class="uk-button uk-button-primary uk-button-small">
        Retour à la liste des utilisateurs
    </a>
</div>

<%- include ('../partials/footer') %>